<div class="main">
  <div class="a00">
    <h1><%= top %></h1>
    <button class="normalButton" onclick="
  fetch('/log', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: 'reload on /<%= page %> ... OK' })
  }).then(() => {
    setTimeout(() => location.href = '/<%= page %>', 100);
  });
">
  ページを再読み込み
</button>
  </div>